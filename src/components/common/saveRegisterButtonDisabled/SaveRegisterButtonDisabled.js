import React from 'react';
import './saveRegisterButtonDisabled.css';

const SaveRegisterButtonDisabled = () => (
  <button type="button" className="Save_register_button_disabled" onClick={() => {}} disabled>Save</button>
  );

export default SaveRegisterButtonDisabled;
